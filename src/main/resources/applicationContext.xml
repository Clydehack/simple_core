<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:aop="http://www.springframework.org/schema/aop"
	xmlns:tx="http://www.springframework.org/schema/tx"
	xmlns:context="http://www.springframework.org/schema/context" 
	xmlns:task="http://www.springframework.org/schema/task"
	xsi:schemaLocation="
			http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
			http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-4.0.xsd
			http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-4.0.xsd
			http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.0.xsd
			http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task-4.0.xsd">
    <bean id="dataSource" class="com.zaxxer.hikari.HikariDataSource" lazy-init="false" destroy-method="close">
        <property name="driverClassName" value="${jdbc.driverClass}"/>
        <property name="jdbcUrl" value="${spring.datasource.url}"/>
        <property name="username" value="${spring.datasource.username}"/>
        <property name="password" value="${spring.datasource.password}"/>
        <property name="autoCommit" value="true"/>
        <property name="readOnly" value="true"/>	<!-- 连接只读数据库时配置为true， 保证安全 -->  
        <property name="transactionIsolation" value="TRANSACTION_Serializable"/>
        <property name="connectionTimeout" value="30000"/>
        <property name="idleTimeout" value="31000"/>
        <!-- 2018-5-19 - 20 后台管理已经改变了一组图片的状态为停用了，但是首页还是现实，查看发现，数据库字段已改变成1了，
        	但是查库操作还是得到原来为0的数据，相当于之前的查库动作呗缓存了，改成31000 30秒左右首页就能有变化了
        	用来设置一个connection在连接池中的存活时间，默认是1800000，即30分钟。如果设置为0，表示存活时间无限大。如果不等于0且小于30秒则会被重置回30分钟 -->
        <property name="maxLifetime" value="31000"/> <!-- 540000 一个连接的生命时长（毫秒），超时而且没被使用则被释放（retired）-->
        <property name="maximumPoolSize" value="${jdbc.pool.maxPoolSize}"/>
        <property name="isolateInternalQueries" value="false"/>
    </bean>
    <!-- 配置事务管理组件 -->
    <!-- 
    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource" />
    </bean>
     -->
    <tx:annotation-driven />
    <!-- 创建SqlSessionFactory，同时指定数据源-->
    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="dataSource" />
    </bean>
    
     <bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
     <!-- <property name="basePackage" value="com.template.ie" /> -->
        <property name="basePackage" value="com.template.ie" />
    </bean>
    
</beans>